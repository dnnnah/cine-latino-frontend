[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["NODE_OPTIONS=--max-old-space-size=4096 npm run build"]

[start]
cmd = "npx serve dist/frontend/browser -l $PORT"
